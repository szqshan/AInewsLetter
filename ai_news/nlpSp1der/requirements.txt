# Core crawler dependencies
aiohttp>=3.8.0
aiofiles>=23.0.0
playwright>=1.40.0
beautifulsoup4>=4.12.0
markdownify>=0.11.0
tqdm>=4.66.0
python-dateutil>=2.8.0
pillow>=10.0.0
requests>=2.31.0

# Anti-detection dependencies
playwright-stealth>=1.0.6
# fake-useragent>=1.4.0  # 未使用，保留为可选

# Elasticsearch dependencies（可选：当前项目未使用，默认不安装）
# elasticsearch[async]>=8.11.0

# Optional: For embeddings and vector search（可选：当前项目未使用，默认不安装）
# sentence-transformers>=2.2.0
# torch>=2.0.0
# numpy>=1.24.0

# Optional: For future recommendation features (not implemented in crawler)
# scikit-learn>=1.3.0